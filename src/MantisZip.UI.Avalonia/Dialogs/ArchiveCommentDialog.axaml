<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:Class="MantisZip.UI.Avalonia.Dialogs.ArchiveCommentDialog"
        x:CompileBindings="False"
        Title="{Binding WinTitle}"
        Width="480"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        CanResize="False"
        ShowInTaskbar="False"
        Background="{DynamicResource ThemeWindowBgBrush}"
        Padding="{DynamicResource SpacingMdThk}">
  <Grid RowDefinitions="Auto,*,Auto,Auto,Auto"
        Margin="{DynamicResource SpacingMdThk}">

    <!-- 第 0 行：提示文本 -->
    <TextBlock Grid.Row="0"
               Text="{Binding PromptText}"
               Margin="0,0,0,8"
               Foreground="{DynamicResource ThemeTextPrimaryBrush}" />

    <!-- 第 1 行：注释多行编辑框 -->
    <TextBox Grid.Row="1"
             x:Name="CommentTextBox"
             AcceptsReturn="True"
             TextWrapping="Wrap"
             ScrollViewer.VerticalScrollBarVisibility="Auto"
             MinHeight="120"
             Padding="{DynamicResource SpacingXsThk}"
             Background="{DynamicResource ThemeSurfaceBgBrush}"
             Foreground="{DynamicResource ThemeTextPrimaryBrush}"
             BorderBrush="{DynamicResource ThemeBorderBrush}"
             BorderThickness="1" />

    <!-- 第 2 行：格式提示（仅 ZIP 支持注释） -->
    <TextBlock Grid.Row="2"
               Text="{Binding ZipOnlyHint}"
               Foreground="{DynamicResource ThemeTextDisabledBrush}"
               FontSize="12"
               Margin="0,8,0,0" />

    <!-- 第 3 行：保存状态文本（默认隐藏，保存时显示） -->
    <TextBlock Grid.Row="3"
               x:Name="SavingStatusBlock"
               Text="{Binding SavingStatusText}"
               Foreground="{DynamicResource ThemeTextSecondaryBrush}"
               FontStyle="Italic"
               VerticalAlignment="Center"
               IsVisible="False"
               Margin="0,12,0,0" />

    <!-- 第 4 行：按钮面板（取消 / 保存） -->
    <StackPanel Grid.Row="4"
                x:Name="ButtonPanel"
                Orientation="Horizontal"
                HorizontalAlignment="Right"
                Spacing="{DynamicResource SpacingXs}"
                Margin="0,12,0,0">
      <Button x:Name="CancelBtn"
              Content="{Binding CancelText}"
              Width="80"
              Height="{DynamicResource ControlHeight}"
              IsCancel="True"
              Click="OnCancelClick"
              Background="{DynamicResource ThemeButtonBgBrush}"
              Foreground="{DynamicResource ThemeTextPrimaryBrush}"
              BorderBrush="{DynamicResource ThemeBorderBrush}" />
      <Button x:Name="SaveBtn"
              Content="{Binding SaveText}"
              Width="80"
              Height="{DynamicResource ControlHeight}"
              IsDefault="True"
              Click="OnSaveClick"
              Background="{DynamicResource ThemeAccentBrush}"
              Foreground="White"
              BorderThickness="0" />
    </StackPanel>
  </Grid>
</Window>
