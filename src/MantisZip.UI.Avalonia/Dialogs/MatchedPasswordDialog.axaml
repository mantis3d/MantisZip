<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:Class="MantisZip.UI.Avalonia.Dialogs.MatchedPasswordDialog"
        x:CompileBindings="False"
        Title="{Binding WinTitle}"
        Width="400"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource ThemeWindowBgBrush}"
        Padding="{DynamicResource SpacingMdThk}">

  <Grid RowDefinitions="Auto,Auto,Auto,Auto,Auto"
        Margin="{DynamicResource SpacingXsThk}">
    <!-- Row 0: 文件名标题行 -->
    <TextBlock Grid.Row="0"
               x:Name="HeaderText"
               FontWeight="Bold"
               FontSize="14"
               Foreground="{DynamicResource ThemeTextPrimaryBrush}"
               Margin="0,0,0,12" />

    <!-- Row 1: 密码显示行：图标 + 掩码/明文密码 + 显示切换按钮 + 复制按钮 -->
    <Border Grid.Row="1"
            Background="{DynamicResource ThemeSurfaceBgBrush}"
            BorderBrush="{DynamicResource ThemeAccentBrush}"
            BorderThickness="1"
            CornerRadius="{DynamicResource BorderRadius}"
            Padding="8,6"
            Margin="0,0,0,8">
      <Grid ColumnDefinitions="Auto,*,Auto,Auto">
        <!-- 钥匙图标 -->
        <PathIcon Grid.Column="0"
                  Data="{StaticResource IconKey}"
                  Width="16"
                  Height="16"
                  Foreground="{DynamicResource ThemeTextPrimaryBrush}"
                  Margin="0,0,6,0"
                  VerticalAlignment="Center" />

        <!-- 密码显示区域：掩码文本与明文文本切换 -->
        <Grid Grid.Column="1"
              VerticalAlignment="Center">
          <TextBlock x:Name="PwdMaskedText"
                     Text="●●●●●●●●●●"
                     FontSize="13"
                     FontWeight="SemiBold"
                     Foreground="{DynamicResource ThemeTextPrimaryBrush}"
                     VerticalAlignment="Center" />
          <TextBlock x:Name="PwdPlainText"
                     FontSize="13"
                     FontWeight="SemiBold"
                     Foreground="{DynamicResource ThemeTextPrimaryBrush}"
                     VerticalAlignment="Center"
                     IsVisible="False" />
        </Grid>

        <!-- 显示/隐藏切换按钮 -->
        <Button Grid.Column="2"
                x:Name="PwdRevealBtn"
                Width="24"
                Height="{DynamicResource ControlHeightXs}"
                Margin="4,0,0,0"
                Padding="0"
                Background="{DynamicResource ThemeButtonBgBrush}"
                BorderBrush="{DynamicResource ThemeBorderBrush}"
                Foreground="{DynamicResource ThemeTextSecondaryBrush}"
                ToolTip.Tip="{Binding RevealTooltip}"
                Click="OnRevealClick">
          <StackPanel>
            <PathIcon x:Name="RevealEyeIcon" Data="{StaticResource IconEye}" Width="14" Height="14" Foreground="{DynamicResource ThemeTextPrimaryBrush}" />
            <PathIcon x:Name="RevealEyeOffIcon" Data="{StaticResource IconEyeOff}" Width="14" Height="14" Foreground="{DynamicResource ThemeTextPrimaryBrush}" IsVisible="False" />
          </StackPanel>
        </Button>

        <!-- 复制密码按钮 -->
        <Button Grid.Column="3"
                x:Name="PwdCopyBtn"
                Width="24"
                Height="{DynamicResource ControlHeightXs}"
                Margin="4,0,0,0"
                Padding="0"
                Background="{DynamicResource ThemeButtonBgBrush}"
                BorderBrush="{DynamicResource ThemeBorderBrush}"
                Foreground="{DynamicResource ThemeTextPrimaryBrush}"
                ToolTip.Tip="{Binding CopyTooltip}"
                Click="OnCopyClick">
          <PathIcon Data="{StaticResource IconCopy}" Width="14" Height="14" Foreground="{DynamicResource ThemeTextPrimaryBrush}" />
        </Button>
      </Grid>
    </Border>

    <!-- Row 2: 匹配规则说明区域（默认隐藏，有规则时显示） -->
    <Border Grid.Row="2"
            x:Name="RulesSection"
            IsVisible="False"
            Margin="22,0,0,4">
      <TextBlock x:Name="RulesText"
                 FontSize="11"
                 Foreground="{DynamicResource ThemeTextSecondaryBrush}"
                 TextWrapping="Wrap" />
    </Border>

    <!-- Row 3: 描述信息区域（默认隐藏，有描述时显示） -->
    <Border Grid.Row="3"
            x:Name="DescriptionSection"
            IsVisible="False"
            Margin="22,0,0,12">
      <TextBlock x:Name="DescriptionText"
                 FontSize="11"
                 Foreground="{DynamicResource ThemeTextSecondaryBrush}"
                 TextWrapping="Wrap" />
    </Border>

    <!-- Row 4: 底部按钮行：使用 / 取消 -->
    <StackPanel Grid.Row="4"
                Orientation="Horizontal"
                HorizontalAlignment="Right"
                Spacing="{DynamicResource SpacingXs}">
      <Button Content="{Binding UseText}"
              Click="OnUseClick"
              Padding="16,6"
              IsDefault="True"
              Background="{DynamicResource ThemeButtonBgBrush}"
              Foreground="{DynamicResource ThemeTextPrimaryBrush}"
              BorderBrush="{DynamicResource ThemeBorderBrush}" />
      <Button Content="{Binding CancelText}"
              Click="OnCancelClick"
              Padding="16,6"
              Background="{DynamicResource ThemeButtonBgBrush}"
              Foreground="{DynamicResource ThemeTextPrimaryBrush}"
              BorderBrush="{DynamicResource ThemeBorderBrush}" />
    </StackPanel>
  </Grid>
</Window>
