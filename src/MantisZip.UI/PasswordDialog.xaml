<Window x:Class="MantisZip.UI.PasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="输入密码" Height="220" Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="#F5F5F5">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding FileName}" FontWeight="Bold" FontSize="14" Margin="0,0,0,4"/>

        <TextBlock Grid.Row="1" Text="密码:" Margin="0,8,0,4"/>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <ComboBox x:Name="PasswordCombo" Grid.Column="0" IsEditable="True" 
                    Text="" FontSize="14" Padding="8,6"
                    KeyDown="PasswordCombo_KeyDown">
                <ComboBoxItem Content="(输入新密码)"/>
            </ComboBox>
        </Grid>

        <CheckBox Grid.Row="3" x:Name="RememberCheckBox" Content="记住密码用于同名文件" 
                 Margin="0,12,0,0" IsChecked="True"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="确定" Click="Ok_Click" Width="80" Height="32" Margin="0,0,8,0" 
                    IsDefault="True" Background="#2196F3" Foreground="White"/>
            <Button Content="取消" Click="Cancel_Click" Width="80" Height="32" 
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>